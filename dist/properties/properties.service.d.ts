import { DataSource, Repository } from 'typeorm';
import { AuthenticatedUser } from '../common/interfaces/authenticated-user.interface';
import { PaginatedResult } from '../common/dto/pagination.dto';
import { Amenity } from '../amenities/entities/amenity.entity';
import { User } from '../users/entities/user.entity';
import { CreatePropertyDto } from './dto/create-property.dto';
import { QueryPropertyDto } from './dto/query-property.dto';
import { PropertyResponseDto } from './dto/property-response.dto';
import { UpdatePropertyDto } from './dto/update-property.dto';
import { Property } from './entities/property.entity';
import { UploadsService } from '../modules/uploads/services/uploads.service';
export declare class PropertiesService {
    private readonly propertyRepo;
    private readonly amenityRepo;
    private readonly userRepo;
    private readonly dataSource;
    private readonly uploadsService;
    constructor(propertyRepo: Repository<Property>, amenityRepo: Repository<Amenity>, userRepo: Repository<User>, dataSource: DataSource, uploadsService: UploadsService);
    create(dto: CreatePropertyDto, user: AuthenticatedUser): Promise<PropertyResponseDto>;
    findAll(query: QueryPropertyDto, user: AuthenticatedUser): Promise<PaginatedResult<PropertyResponseDto>>;
    findOne(id: string, user: AuthenticatedUser): Promise<PropertyResponseDto>;
    update(id: string, dto: UpdatePropertyDto, user: AuthenticatedUser): Promise<PropertyResponseDto>;
    remove(id: string, user: AuthenticatedUser): Promise<void>;
    private getPropertyOrFail;
    private applyRoleScope;
    private applyFilters;
    private assertCanRead;
    private assertCanModify;
    private assertStatusTransitionAllowed;
    private resolveAssignedSellerId;
    private validateAssignedSeller;
    private validateAmenities;
    private resolvePublishedAt;
    private syncImages;
    private normalizeImages;
    private syncAmenities;
    private syncFeatures;
    private toNumeric;
    private toUserSummary;
    private toGeoSummary;
    toResponse(property: Property): PropertyResponseDto;
}
